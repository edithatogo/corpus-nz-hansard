# Track dependency_extras_policy_20260609 Context

Keep the base corpus pipeline lightweight while making endpoint-specific dependencies discoverable and installable.

Initial documentation and grouped requirement files exist in `docs/dependency-policy.md` and `requirements/`.
