# Track ud_conllu_endpoint_20260609 Context

Create linguistic annotation artifacts with token-to-source alignment.

This track should use `stanza`, `spacy`, `conllu`, or `pyconll` only after the annotation scope and validation rules are explicit.
